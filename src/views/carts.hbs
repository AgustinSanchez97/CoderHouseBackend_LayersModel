<div>
    <h1>Productos Agregados</h1>

    <div class="p2">

        <table class="table">
            <thead class="table-dark">
                <tr>
                    <th>#</th>        
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
            {{#each carts}}
            <tr>
                <td>Cart #{{this.id}} </td>

                <td>
                    <a href="/editCart/{{this.id}}" class="btn btn-success">Edit</a>                                                        
                    <button class="deleteCart btn btn-danger" id="{{this.id}}" data-id="{{@index}}">Delete</button>

                </td>
            </tr>
            {{/each}}
            </tbody>

        </table>
    </div>

    <hr>
    <div>
        <form class="row p-3" action="/api/carts" method="POST">            
            <input type="submit" value="CREAR NUEVO CARRO" class="btn btn-success">
        </form>
    </div>
</div>

<script src="/js/carts.js"></script>

